{
  "name": "ds-example",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/demo",
    "build:lib": "npm run build --workspace=packages/ui",
    "build": "npm run build:lib && npm run build --workspace=apps/demo",
    "preview": "npm run preview --workspace=apps/demo"
  }
}
